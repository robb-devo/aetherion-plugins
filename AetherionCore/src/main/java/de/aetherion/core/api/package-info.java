/**
 * Cross-plugin service interfaces and {@link de.aetherion.core.api.AetherServices} registry.
 * First-party plugins register implementations on enable. FancyNpcs, WorldEdit,
 * DiscordSRV, and TAB stay on reflection — they are external plugins.
 */
package de.aetherion.core.api;
